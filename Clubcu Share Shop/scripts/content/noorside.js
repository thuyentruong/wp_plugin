var parentClass = ".item-project";
var productList = document.querySelectorAll(parentClass);
var expandHeight = null;
var beforeNode = ".productlink";

buildShareShopElements(productList, beforeNode, expandHeight);